# 手动 Import 备用

**推荐：Postman Native Git 自动同步**，见 [postman/README.md](../../postman/README.md)。

仅在无法使用 Native Git 时，才手动 Import 本目录下：

- `IHope.postman_collection.json`
- `IHope.environment.json`

不要与 Native Git 集合同时存在，否则会重复。
